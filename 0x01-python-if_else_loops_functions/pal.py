#!/usr/bin/python3
str = "wallaw"
str2 = "whatever"

def rev(str):
    
    

def cmp(str):
    
    
start = 0
end = len(str)

def ispal(str, start, end):
    if start <= len(str)
        if str[start] == str[end]
            return ispal(str, start + 1, end - 1)
        else if str[start] != str[end]
            return 0
    else
        return 1
